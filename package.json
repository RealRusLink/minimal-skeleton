{
  "name": "minimal-skeleton",
  "version": "0.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "app.ts",
  "scripts": {
    "test": "vitest run",
    "test:db": "vitest",
    "build": "tsc -p tsconfig.json",
    "start": "node ./dist/app.js",
    "fast": "npm run build && timeout 5 && npm run start",
    "clearn": "powershell -Command \"Get-ChildItem -Path ./src -Include *.js -Recurse | Remove-Item -Force\""
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/pg": "^8.20.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.0",
    "dotenv": "^17.4.2",
    "hono": "^4.12.14",
    "logger": "github:RealRusLink/logger",
    "pg": "^8.20.0"
  }
}
