{
  "name": "minimal-skeleton",
  "version": "0.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "test": "vitest run",
    "test:db": "vitest",
    "build": "tsc -p tsconfig.json",
    "start": "node ./dist/server.js",
    "fast": "npm run build && timeout 5 && npm run start"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.6.0",
    "ts-jest": "^29.4.9",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "@fastify/static": "^9.1.0",
    "fastify": "^5.8.4"
  }
}
